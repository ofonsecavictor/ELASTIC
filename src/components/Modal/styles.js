import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

    modalTitle:{
        color:'#fc034e',
        fontWeight:'bold',
        fontSize: 18,
        marginLeft: 20,
        marginTop: 40,
    },
})