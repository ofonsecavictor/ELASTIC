import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    checkContainer:{
        flexDirection: 'row',
        marginTop:10,
        marginLeft: 15,
        marginBottom: 10,
    },

    title:{
        marginTop:10,
        marginLeft: 15,
        fontWeight: 'bold',
    },

})